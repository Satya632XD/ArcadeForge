{
  "name": "code-game-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm --prefix api install && npm --prefix web install",
    "dev:api": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev"
  }
}
